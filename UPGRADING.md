# Upgrading

This doc will show guide you on the important changes between major versions.

## v1 to v2

Make sure you updated your function calls to the follow plural versions

```sh
append() -> appends()
include() -> includes()
```